

run:
##    warn: true
    echo: true
#    hide: false

# 默认用户名密码
user: root
connect_kwargs:
    password: '111111'


hosts:
    list:
        - name: local
          host: 192.168.0.80
          user: long
          pass: '111111'
        - host: 192.168.0.82
        - host: 192.168.0.83

    disk:
        - /mnt/disk1
        - /mnt/disk2

install:
    source: http://download.redis.io/releases/redis-5.0.0.tar.gz
    compile: /tmp/redis
    path: /opt/redis
    aaaa: 111
