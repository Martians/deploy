
# 用于进行 common 库的调试

run:
##    warn: true
    echo: true
#    hide: false

# 默认用户名密码
user: root
connect_kwargs:
    password: '111111'


hosts:
    list:
        - name: local
          host: 192.168.0.80
          user: long
          pass: '111111'
          type: control
        - host: 192.168.0.81
        - host: 192.168.0.82
        - host: 192.168.0.83
    disk:
        - /mnt/disk1
        - /mnt/disk2

install:
    source: https://archive.apache.org/dist/ignite/2.6.0/apache-ignite-hadoop-2.6.0-bin.zip
    parent:  /opt
    cluster:
        directory: 'cluster'
        instance: 5
        portbase: 7000
        replica: 0